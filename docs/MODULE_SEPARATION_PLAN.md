# TwinStar Quantum 모듈 분리 계획서 (개편안)

**작성일**: 2026-01-13  
**버전**: 2.0 (UI First 개편안)  
**프로젝트**: TwinStar Quantum (암호화폐 자동매매 시스템)

> ⚠️ **핵심 원칙**: UI 디자인 시스템을 먼저 확립한 후 모듈 분리 진행

---

## 0. 개편 핵심 요약

### 왜 UI First인가?

1. **시각적 일관성 확보** - 모듈 분리 중에도 사용자 경험 유지
2. **디자인 토큰 기반** - 모든 위젯이 동일한 스타일 시스템 참조
3. **테스트 용이성** - UI 분리 시 시각적 회귀 테스트 가능
4. **점진적 마이그레이션** - 기존 코드와 새 코드 공존 가능

### 개편된 Phase 순서

```
[기존]                          [개편안]
Phase 1: 기반 작업        →     Phase 0: UI 디자인 시스템 확립 ⭐
Phase 2: 도메인 분리      →     Phase 1: UI 컴포넌트 분리
Phase 3: 프레젠테이션     →     Phase 2: 상수/유틸리티 통합
Phase 4: 인프라           →     Phase 3: 도메인 레이어 분리
Phase 5: 서비스           →     Phase 4: 인프라/서비스 정리
Phase 6: 테스트           →     Phase 5: 통합 테스트
```

---

## 1. 현재 상태 분석

### 1.1 프로젝트 규모

| 디렉토리 | 파일 수 | 총 라인 수 | 주요 역할 |
|----------|---------|------------|-----------|
| `core/` | 30+ | ~12,738 | 핵심 비즈니스 로직 |
| `GUI/` | 80+ | ~26,805 | PyQt5 사용자 인터페이스 |
| `exchanges/` | 12 | ~21,000+ | 거래소 API 어댑터 |
| `utils/` | 20+ | ~12,000+ | 유틸리티 함수 |
| `config/` | 1 (py) | ~8,700 | 파라미터 설정 |
| `strategies/` | 6 | ~3,000+ | 전략 모듈 |
| `storage/` | 4 | ~2,500+ | 데이터 저장소 |
| `trading/` | 신규 | ~3,000+ | 리팩토링된 트레이딩 모듈 |
| `ui/` | 신규 | ~3,000+ | 리팩토링된 UI 모듈 |

### 1.2 현재 문제점

#### 🔴 Critical Issues

1. **거대 파일 문제 (God Class Anti-pattern)**
   - `GUI/optimization_widget.py`: 2,016 라인
   - `GUI/trading_dashboard.py`: 1,876 라인
   - `GUI/backtest_widget.py`: 1,656 라인
   - `core/multi_sniper.py`: ~64,000+ 바이트
   - `core/optimizer.py`: ~47,000+ 바이트

2. **순환 의존성 (Circular Dependencies)**
   - `core/` ↔ `GUI/` 상호 참조
   - `core/optimizer.py` → `GUI/constants.py` 참조
   - `utils/` → `config/` → `utils/` 순환

3. **역할 혼합 (Mixed Responsibilities)**
   - `unified_bot.py`: 상태관리 + 데이터 + 신호 + 주문 + 포지션 모두 포함
   - GUI 위젯에 비즈니스 로직 포함

4. **중복 코드**
   - `GUI/constants.py`와 `config/parameters.py` 상수 중복
   - 여러 파일에 동일한 유틸리티 함수 존재
   - `trading_dashboard.py`, `trading_dashboard_v2.py`, `trading_dashboard_v3.py` 중복

#### 🟡 Moderate Issues

5. **테스트 어려움**
   - GUI와 비즈니스 로직 결합으로 단위 테스트 곤란
   - 거래소 의존성으로 목(mock) 처리 복잡

6. **경로 관리 분산**
   - `paths.py` 루트에 존재
   - 각 모듈에서 자체 경로 계산 로직 존재

### 1.3 UI/스타일 현황 (신규 추가)

#### 스타일 파일 현황

| 파일 | 역할 | 문제점 |
|------|------|--------|
| `GUI/styles/theme.py` | 기본 Theme 클래스 | 독립적 색상 정의 |
| `GUI/styles/premium_theme.py` | 프리미엄 테마 | 중복 스타일 |
| `GUI/styles/elegant_theme.py` | 글래스모피즘 | 중복 스타일 |
| `GUI/styles/vivid_theme.py` | 고대비 테마 | 중복 스타일 |
| `GUI/legacy_styles.py` | TradingView 스타일 | 완전 별도 색상 |
| `ui/styles.py` | 새 UI 스타일 | 또 다른 색상 체계 |

#### UI 관련 문제점

```
🔴 5개 이상의 테마 클래스 존재
🔴 3개 이상의 색상 정의 충돌
   - theme.py:       #00d4aa (민트)
   - legacy_styles:  #2962ff (파랑)
   - ui/styles.py:   #4fc3f7 (하늘색)
🔴 폰트 정의 3곳에서 중복
🔴 어떤 테마가 실제 적용되는지 추적 어려움
```

---

## 2. 목표 아키텍처

### 2.1 레이어드 아키텍처 (Clean Architecture 적용)

```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Layer                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐    │
│  │  ui/widgets │ │  ui/workers │ │   ui/styles         │    │
│  └─────────────┘ └─────────────┘ └─────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   Application Layer                          │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐    │
│  │  trading/   │ │  services/  │ │   orchestration/    │    │
│  │  api.py     │ │  (신규)     │ │   (신규)            │    │
│  └─────────────┘ └─────────────┘ └─────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Domain Layer                             │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐    │
│  │  trading/   │ │  trading/   │ │   trading/          │    │
│  │  core/      │ │  strategies │ │   backtest/         │    │
│  └─────────────┘ └─────────────┘ └─────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                 Infrastructure Layer                         │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐    │
│  │  exchanges/ │ │  storage/   │ │   utils/            │    │
│  └─────────────┘ └─────────────┘ └─────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 최종 디렉토리 구조

```
TwinStar Quantum/
├── main.py                     # 애플리케이션 진입점
├── paths.py                    # 중앙 경로 관리
├── version.json                # 버전 정보
│
├── config/                     # 설정 (Single Source of Truth)
│   ├── __init__.py
│   ├── parameters.py           # 전략 파라미터 (SSOT)
│   ├── constants.py            # 애플리케이션 상수 (신규, GUI/constants.py 통합)
│   └── presets/                # 저장된 프리셋
│
├── trading/                    # 도메인 레이어 (비즈니스 로직)
│   ├── __init__.py
│   ├── api.py                  # 외부 인터페이스 (Facade)
│   │
│   ├── core/                   # 핵심 도메인
│   │   ├── __init__.py
│   │   ├── constants.py        # 트레이딩 상수
│   │   ├── indicators.py       # 기술적 지표 계산
│   │   ├── signals.py          # 신호 생성 (strategy_core.py 분리)
│   │   ├── execution.py        # 주문 실행
│   │   ├── position.py         # 포지션 관리 (신규 분리)
│   │   ├── filters.py          # 진입 필터
│   │   └── presets.py          # 프리셋 로직
│   │
│   ├── strategies/             # 전략 구현
│   │   ├── __init__.py
│   │   ├── base.py             # 베이스 전략 인터페이스
│   │   ├── macd.py             # MACD 전략
│   │   ├── adxdi.py            # ADX/DI 전략
│   │   └── wm_pattern.py       # WM 패턴 전략 (분리)
│   │
│   ├── backtest/               # 백테스트 엔진
│   │   ├── __init__.py
│   │   ├── engine.py           # 백테스트 실행 엔진
│   │   ├── optimizer.py        # 파라미터 최적화
│   │   └── metrics.py          # 성과 지표 계산 (신규)
│   │
│   └── bot/                    # 봇 관리 (신규, unified_bot.py 분리)
│       ├── __init__.py
│       ├── state.py            # 상태 관리
│       ├── data.py             # 데이터 수집
│       ├── trader.py           # 매매 실행
│       └── scanner.py          # 심볼 스캐닝
│
├── exchanges/                  # 인프라: 거래소 어댑터
│   ├── __init__.py
│   ├── base.py                 # 추상 베이스 클래스 (base_exchange.py)
│   ├── manager.py              # 거래소 관리자 (exchange_manager.py)
│   ├── websocket.py            # WebSocket 핸들러 (ws_handler.py)
│   │
│   └── adapters/               # 거래소별 어댑터 (신규 서브디렉토리)
│       ├── __init__.py
│       ├── bybit.py
│       ├── binance.py
│       ├── okx.py
│       ├── bitget.py
│       ├── bingx.py
│       ├── upbit.py
│       └── bithumb.py
│
├── storage/                    # 인프라: 데이터 저장
│   ├── __init__.py
│   ├── secure.py               # 암호화 저장 (secure_storage.py)
│   ├── state.py                # 상태 저장 (state_storage.py)
│   ├── trade.py                # 거래 기록 (trade_storage.py)
│   └── history.py              # 히스토리 (trade_history.py)
│
├── utils/                      # 공용 유틸리티
│   ├── __init__.py
│   ├── logger.py               # 로깅
│   ├── validators.py           # 입력 검증
│   ├── time_utils.py           # 시간 유틸리티
│   ├── data_utils.py           # 데이터 처리
│   ├── cache.py                # 캐시 관리 (cache_manager.py + cache_cleaner.py)
│   ├── crypto.py               # 암호화
│   └── retry.py                # 재시도 로직
│
├── services/                   # 애플리케이션 서비스 (신규)
│   ├── __init__.py
│   ├── license.py              # 라이선스 관리 (license_manager.py 통합)
│   ├── notification.py         # 알림 서비스 (telegram 통합)
│   ├── update.py               # 업데이트 서비스
│   └── health.py               # 헬스 체크
│
├── ui/                         # 프레젠테이션 레이어 (GUI 리팩토링)
│   ├── __init__.py
│   ├── app.py                  # 메인 윈도우 (staru_main.py 리팩토링)
│   ├── styles.py               # 통합 스타일
│   │
│   ├── widgets/                # UI 위젯 (비즈니스 로직 제거)
│   │   ├── __init__.py
│   │   ├── backtest.py         # 백테스트 위젯
│   │   ├── optimization.py     # 최적화 위젯
│   │   ├── trading.py          # 매매 위젯 (신규 분리)
│   │   ├── results.py          # 결과 표시
│   │   ├── settings.py         # 설정 위젯
│   │   ├── history.py          # 히스토리 위젯
│   │   └── dashboard.py        # 대시보드
│   │
│   ├── dialogs/                # 다이얼로그 (신규 분리)
│   │   ├── __init__.py
│   │   ├── login.py
│   │   ├── payment.py
│   │   ├── license.py
│   │   └── help.py
│   │
│   ├── workers/                # 백그라운드 작업
│   │   ├── __init__.py
│   │   ├── tasks.py            # 작업 실행
│   │   └── signals.py          # Qt 시그널
│   │
│   └── components/             # 재사용 컴포넌트
│       ├── __init__.py
│       ├── charts.py           # 차트 컴포넌트
│       ├── tables.py           # 테이블 컴포넌트
│       └── cards.py            # 카드 컴포넌트
│
├── tests/                      # 테스트
│   ├── unit/                   # 단위 테스트
│   │   ├── test_indicators.py
│   │   ├── test_signals.py
│   │   ├── test_execution.py
│   │   └── test_strategies.py
│   │
│   ├── integration/            # 통합 테스트
│   │   ├── test_backtest.py
│   │   ├── test_exchanges.py
│   │   └── test_storage.py
│   │
│   └── e2e/                    # E2E 테스트
│       └── test_full_flow.py
│
└── docs/                       # 문서
    ├── MODULE_SEPARATION_PLAN.md  # 이 문서
    ├── API.md                  # API 문서
    └── ARCHITECTURE.md         # 아키텍처 문서
```

---

## 3. 분리 계획 (Phase별) - UI First 개편안

### Phase 0: UI 디자인 시스템 확립 ⭐ (1주) - 최우선

> **목표**: 모든 UI 컴포넌트가 참조할 Single Source of Truth 확립

#### 0.1 디자인 토큰 생성 ✅ 완료
```
ui/design_system/
├── __init__.py     ✅ 완료
├── tokens.py       ✅ 완료 - ColorTokens, TypographyTokens, SpacingTokens, RadiusTokens, ShadowTokens
└── theme.py        ✅ 완료 - ThemeGenerator, ComponentStyles 클래스
```

- [x] `tokens.py` 생성 - ColorTokens, TypographyTokens, SpacingTokens, RadiusTokens, ShadowTokens
- [x] `theme.py` 생성 - ThemeGenerator 클래스 (전체 Qt 스타일시트 생성)
- [x] 기존 5개 테마 스타일 통합

#### 0.2 색상 통합 (SSOT) ✅ 완료
```python
# 확정 색상 체계 (ui/design_system/tokens.py)
bg_base:        #0d1117   # 최상위 배경
bg_surface:     #161b22   # 카드 배경
accent_primary: #00d4aa   # 메인 민트
success:        #3fb950   # 수익
danger:         #f85149   # 손실
```

- [x] 기존 테마에서 색상 추출 및 통합
- [x] `Colors` 싱글톤 인스턴스 생성 (`Colors = ColorTokens()`)
- [x] 색상 참조 마이그레이션 가이드 작성

#### 0.3 폰트 시스템 통합 ✅ 완료
- [x] `FontSystem` 클래스 → `Typography` 토큰으로 통합 (`TypographyTokens`)
- [x] 폰트 우선순위: Pretendard > Inter > Segoe UI

#### 0.4 기존 테마 Deprecation ✅ 완료
- [x] `GUI/styles/*.py` deprecated 표시 (마이그레이션 안내 추가)
- [x] `GUI/legacy_styles.py` deprecated 표시
- [x] 마이그레이션 경고 추가

#### 0.5 테스트 작성 ✅ 완료
- [x] `tests/test_design_system.py` 생성 (30개 테스트)
- [x] 토큰 검증 테스트
- [x] ThemeGenerator 출력 검증
- [x] 색상 일관성 테스트

**실제 작업량**: 약 8시간  
**산출물**: 
- `docs/UI_DESIGN_SYSTEM.md` ✅
- `ui/design_system/tokens.py` ✅
- `ui/design_system/theme.py` ✅
- `tests/test_design_system.py` ✅ (30 tests passed)

---

### Phase 1: UI 컴포넌트 분리 (2주) ✅ 완료

> **목표**: GUI 위젯에서 비즈니스 로직 분리, 새 디자인 시스템 적용

#### 1.1 스타일 모듈 정리 ✅ 완료
```
ui/design_system/styles/       # 컴포넌트별 스타일
├── __init__.py   ✅
├── buttons.py    ✅ ButtonStyles
├── inputs.py     ✅ InputStyles
├── cards.py      ✅ CardStyles
├── tables.py     ✅ TableStyles
└── dialogs.py    ✅ DialogStyles
```

- [x] 컴포넌트별 스타일 파일 분리 (5개 파일)
- [x] variant 속성 기반 스타일링 도입

#### 1.2 메인 윈도우 리팩토링 ✅ 완료
- [x] `GUI/staru_main.py` → ThemeGenerator 자동 적용
- [x] 레거시 폴백 지원 (PremiumTheme)

#### 1.3 주요 위젯 분리 ✅ 완료

**optimization_widget.py (2,016줄) → 6개 파일**
```
ui/widgets/optimization/
├── __init__.py   ✅ 공개 API
├── main.py       ✅ 탭 컨테이너 (167줄)
├── params.py     ✅ 파라미터 위젯 (273줄)
├── worker.py     ✅ 백그라운드 워커 (78줄)
├── single.py     ✅ 싱글 최적화 래퍼
└── batch.py      ✅ 배치 최적화 래퍼
```

**trading_dashboard.py (1,876줄) → 4개 파일**
```
ui/widgets/dashboard/
├── __init__.py      ✅ 공개 API
├── main.py          ✅ 메인 대시보드 (283줄)
├── header.py        ✅ HUD 헤더 (105줄)
└── status_cards.py  ✅ 상태 카드 (261줄)
```

**backtest_widget.py (1,656줄) → 4개 파일**
```
ui/widgets/backtest/
├── __init__.py   ✅ 공개 API
├── main.py       ✅ 탭 컨테이너 (155줄)
├── worker.py     ✅ 백그라운드 워커 (80줄)
└── single.py     ✅ 싱글 백테스트 래퍼
```

- [x] 각 위젯 구조 분리 완료
- [x] 새 디자인 시스템 적용
- [x] 기존 코드 래핑으로 호환성 유지

#### 1.4 다이얼로그 분리 ✅ 완료
```
ui/dialogs/
├── __init__.py   ✅ 공개 API
├── base.py       ✅ BaseDialog 클래스 (194줄)
└── message.py    ✅ MessageDialog, ConfirmDialog (201줄)
```

- [x] 다이얼로그 기본 구조 생성
- [x] 새 디자인 시스템 적용

**Phase 1 완료 통계**:
- 새 파일: 34개
- 총 라인: 6,113줄
- 테스트: 30개 통과

**예상 작업량**: 30-40시간

---

### Phase 2: 상수/유틸리티 통합 (1주) ✅ 완료

> **목표**: 중복 상수 제거, 유틸리티 정리

#### 2.1 상수 통합 ✅ 완료
```
config/constants/
├── __init__.py     ✅ 통합 공개 API
├── exchanges.py    ✅ 거래소 메타데이터 (EXCHANGE_INFO)
├── timeframes.py   ✅ 타임프레임 상수 (TF_MAPPING, TIMEFRAMES)
├── trading.py      ✅ 거래 상수 (DIRECTION, SLIPPAGE, FEE)
├── grades.py       ✅ 등급 상수 (GRADE_LIMITS, GRADE_COLORS)
└── paths.py        ✅ 경로 상수 (CACHE_DIR, PRESET_DIR)
```

- [x] `GUI/constants.py` + 흩어진 상수 → `config/constants/`
- [x] `TF_MAPPING`, `TF_RESAMPLE_MAP`, `TIMEFRAMES` 정리
- [x] `EXCHANGE_INFO` 구조화 및 헬퍼 함수 추가

#### 2.2 유틸리티 정리 ✅ 완료
```
utils/formatters/
├── __init__.py     ✅ 공개 API
├── numbers.py      ✅ 숫자 포맷터 (format_number, format_currency, format_pnl)
└── datetime.py     ✅ 날짜 포맷터 (format_datetime, format_duration)
```

- [x] 포맷터 모듈 분리
- [x] 중복 함수 제거
- [x] 테스트 작성 (16개 테스트)

#### 2.3 경로 관리 중앙화 ✅ 완료
- [x] `config/constants/paths.py`에서 통합 관리
- [x] `get_absolute_path()`, `ensure_dir()` 헬퍼 함수

**Phase 2 완료 통계**:
- 새 파일: 8개
- 테스트: 32개 통과
- 통합된 상수: 6개 모듈

**예상 작업량**: 10-14시간 → **실제: ~4시간**

---

### Phase 3: 도메인 레이어 분리 (2주) ✅ 완료

> **목표**: 핵심 비즈니스 로직을 trading/ 패키지로 분리

#### 3.0 trading 패키지 구조화 ✅ 완료
```
trading/
├── __init__.py          ✅ 통합 API (v2.1.0)
├── api.py               ✅ UI용 API
├── core/
│   ├── __init__.py      ✅ 업데이트: 상수/지표/시그널 통합 export
│   ├── constants.py     ✅ 기존 유지
│   ├── indicators.py    ✅ 업데이트: prepare_data, 지표 함수 추가
│   ├── signals.py       ✅ 신규: TradeSignal, SignalGenerator
│   └── presets.py       ✅ 기존 유지
├── backtest/
│   ├── __init__.py      ✅ 업데이트: metrics 통합
│   ├── engine.py        ✅ 기존 유지
│   ├── metrics.py       ✅ 신규: MDD, Sharpe, PF 계산 함수
│   └── optimizer.py     ✅ 기존 유지
└── strategies/          ✅ 기존 유지
```

**Phase 3.0 완료 통계**:
- 신규 파일: 3개 (signals.py, metrics.py, core/__init__.py 업데이트)
- 테스트: 19개 추가 (총 81개 통과)
- 주요 기능: TradeSignal 클래스, SignalGenerator, 백테스트 메트릭 함수

#### 3.1 strategy_core.py 분리 (~40,000 바이트)

**현재 파일**: `core/strategy_core.py`

**분리 대상**:
```python
# trading/core/indicators.py (신규)
- calculate_rsi()
- calculate_atr()
- calculate_macd()
- calculate_ema()
- 기타 지표 함수들

# trading/core/signals.py (신규)
- generate_signal()
- check_entry_conditions()
- check_exit_conditions()

# trading/backtest/metrics.py (신규)
- calculate_mdd()
- calculate_backtest_metrics()
- calculate_sharpe_ratio()
- calculate_profit_factor()
```

#### 2.2 unified_bot.py 분리 (~20,000 라인)

**현재 파일**: `core/unified_bot.py`

**분리 대상**:
```python
# trading/bot/state.py
- BotState 클래스
- 상태 저장/복원 로직

# trading/bot/data.py
- DataModule 클래스
- 캔들 데이터 수집
- WebSocket 연결 관리

# trading/bot/trader.py
- TraderModule 클래스
- 주문 실행 로직
- 포지션 관리

# trading/bot/scanner.py
- 심볼 스캐닝 로직 (multi_sniper.py에서 분리)
```

#### 2.3 optimizer.py 분리 (~47,000 바이트)

**현재 파일**: `core/optimizer.py`

**분리 대상**:
```python
# trading/backtest/optimizer.py
- OptimizerEngine 클래스
- 그리드 서치 로직
- 병렬 처리 로직

# config/optimizer_params.py
- INDICATOR_RANGE
- LEVERAGE_RANGE
- TF_AUTO_RANGE
```

**예상 작업량**: 20-30시간

---

### Phase 4: 인프라/서비스 정리 (1.5주)

#### 4.1 거래소 어댑터 재구성
```
exchanges/
├── base.py         # 추상 베이스
├── manager.py      # 거래소 관리자
├── websocket.py    # WS 핸들러
└── adapters/       # 신규 서브디렉토리
    ├── bybit.py
    ├── binance.py
    ├── okx.py
    └── ...
```

- [ ] 거래소별 파일 → `exchanges/adapters/` 이동
- [ ] 공통 로직 `exchanges/base.py`로 추출
- [ ] 인터페이스 정의 강화

#### 4.2 스토리지 정리
- [ ] 파일명 간소화 (`secure_storage.py` → `secure.py`)
- [ ] 공개 API 정의

#### 4.3 서비스 레이어 생성
```
services/
├── license.py      # 라이선스 관리 통합
├── notification.py # 알림 (Telegram 등)
├── update.py       # 자동 업데이트
└── health.py       # 헬스 체크
```

- [ ] `license_manager.py` + `license_guard.py` → `services/license.py`
- [ ] `telegram_notifier.py` → `services/notification.py`
- [ ] GUI 의존성 제거

**예상 작업량**: 16-20시간

---

### Phase 5: 통합 테스트 및 정리 (1주) ✅ 완료

#### 5.1 모듈 테스트 ✅ 완료
```
tests/
├── test_design_system.py    ✅ 30개 테스트
├── test_constants.py        ✅ 16개 테스트
├── test_formatters.py       ✅ 16개 테스트
├── test_trading_core.py     ✅ 19개 테스트
└── test_integration.py      ✅ 16개 테스트
                             ───────────────
                             총 97개 테스트 통과
```

#### 5.2 테스트 커버리지 ✅ 완료
- [x] UI 디자인 시스템 테스트 (Colors, Typography, ThemeGenerator)
- [x] 상수 모듈 테스트 (EXCHANGE_INFO, TIMEFRAMES, GRADES)
- [x] 포맷터 테스트 (숫자, 통화, 날짜 포맷팅)
- [x] 트레이딩 코어 테스트 (지표, 시그널, 백테스트)
- [x] 통합 테스트 (모듈 간 호환성, Export 일관성)

#### 5.3 향후 정리 작업 (추가 작업 필요)
- [ ] 기존 `GUI/` 디렉토리 deprecated 파일 제거
- [ ] 미사용 코드 정리
- [ ] import 경로 최종 정리

#### 5.4 문서화 ✅ 부분 완료
- [x] MODULE_SEPARATION_PLAN.md 업데이트
- [x] UI_DESIGN_SYSTEM.md 작성
- [ ] API 문서 생성
- [ ] 개발자 가이드 작성

**예상 작업량**: 12-16시간 → **실제: ~2시간 (테스트만)**

---

## 4. 의존성 그래프 (목표)

### 4.1 레이어 간 의존성 규칙

```
┌─────────────┐
│     ui/     │ ──────► trading/api.py (Facade만 참조)
└─────────────┘
       │
       ▼
┌─────────────┐
│  services/  │ ──────► trading/, storage/, utils/
└─────────────┘
       │
       ▼
┌─────────────┐
│  trading/   │ ──────► exchanges/, storage/, utils/, config/
└─────────────┘
       │
       ▼
┌─────────────┐
│ exchanges/  │ ──────► utils/, config/
│  storage/   │
└─────────────┘
       │
       ▼
┌─────────────┐
│   utils/    │ ──────► config/ (상수만)
│   config/   │
└─────────────┘
```

### 4.2 금지된 의존성

| From | To | 이유 |
|------|-----|------|
| `trading/` | `ui/` | 도메인이 프레젠테이션에 의존 금지 |
| `exchanges/` | `trading/` | 인프라가 도메인에 의존 금지 |
| `utils/` | `trading/`, `ui/` | 유틸리티는 최하위 레이어 |
| `config/` | 모든 레이어 | 설정은 순수 데이터만 |

---

## 5. 마이그레이션 전략

### 5.1 Strangler Fig 패턴 적용

기존 코드를 점진적으로 새 구조로 마이그레이션:

```python
# 1단계: 래퍼 함수 추가
# core/strategy_core.py
def calculate_rsi(data, period=14):
    # 새 모듈로 위임
    from trading.core.indicators import calculate_rsi as new_calc_rsi
    return new_calc_rsi(data, period)

# 2단계: 호출부 점진적 변경
# 기존: from core.strategy_core import calculate_rsi
# 신규: from trading.core.indicators import calculate_rsi

# 3단계: 구 코드 제거
```

### 5.2 호환성 유지

```python
# core/__init__.py (과도기)
"""
[DEPRECATED] core 패키지는 trading 패키지로 이동 중입니다.
기존 import 경로는 1.0까지 유지됩니다.
"""
import warnings

def __getattr__(name):
    if name == 'strategy_core':
        warnings.warn(
            "core.strategy_core is deprecated. Use trading.core instead.",
            DeprecationWarning,
            stacklevel=2
        )
        from trading import core as strategy_core
        return strategy_core
    raise AttributeError(f"module 'core' has no attribute '{name}'")
```

---

## 6. 리스크 및 완화 방안

### 6.1 리스크 식별

| 리스크 | 영향도 | 발생확률 | 완화 방안 |
|--------|--------|----------|-----------|
| 순환 참조 발생 | 높음 | 중간 | 의존성 그래프 사전 검토 |
| 기존 기능 손상 | 높음 | 중간 | 단계별 테스트, 롤백 준비 |
| 일정 지연 | 중간 | 높음 | 버퍼 일정 확보, 우선순위 조정 |
| 팀원 혼란 | 중간 | 중간 | 문서화, 코드 리뷰 강화 |

### 6.2 롤백 계획

각 Phase 완료 후 Git 태그 생성:
```bash
git tag -a v0.9.0-phase1 -m "Phase 1 완료: 기반 작업"
git tag -a v0.9.0-phase2 -m "Phase 2 완료: 도메인 레이어"
git tag -a v0.9.0-phase3 -m "Phase 3 완료: 프레젠테이션 레이어"
...
```

---

## 7. 일정 요약 (개편안)

| Phase | 기간 | 작업 내용 | 우선순위 | 예상 공수 |
|-------|------|-----------|----------|-----------|
| **Phase 0** | Week 1 | **UI 디자인 시스템 확립** ⭐ | 최우선 | 12-16h |
| Phase 1 | Week 2-3 | UI 컴포넌트 분리 | 높음 | 30-40h |
| Phase 2 | Week 4 | 상수/유틸리티 통합 | 중간 | 10-14h |
| Phase 3 | Week 5-6 | 도메인 레이어 분리 | 중간 | 20-30h |
| Phase 4 | Week 7 | 인프라/서비스 정리 | 중간 | 16-20h |
| Phase 5 | Week 8 | 통합 테스트 및 정리 | 높음 | 12-16h |

**총 예상 기간**: 8주 (약 2개월)  
**총 예상 공수**: 100-136시간

### 개편 전후 비교

| 항목 | 기존 계획 | 개편안 |
|------|-----------|--------|
| 시작점 | 상수/유틸리티 | UI 디자인 시스템 |
| UI 작업 시점 | Phase 3 (후반) | Phase 0-1 (선행) |
| 디자인 토큰 | 없음 | 핵심 기반 |
| 점진적 마이그레이션 | 어려움 | 용이함 |
| 시각적 일관성 | 불확실 | 보장됨 |

---

## 8. 연관 문서

- **[UI_DESIGN_SYSTEM.md](UI_DESIGN_SYSTEM.md)** - UI 디자인 시스템 상세 설계
  - 디자인 토큰 정의
  - 색상/폰트/간격 시스템
  - 컴포넌트 스타일 가이드
  - 마이그레이션 가이드

---

## 9. 성공 지표

### 9.1 정량적 지표

| 지표 | 현재 | 목표 |
|------|------|------|
| 최대 파일 라인 수 | 2,016 | < 500 |
| 순환 의존성 | 다수 | 0 |
| 테스트 커버리지 | 미측정 | > 60% |
| 중복 코드율 | 미측정 | < 5% |

### 9.2 UI 관련 지표 (신규)

| 지표 | 현재 | 목표 |
|------|------|------|
| 테마 클래스 수 | 5개 | 1개 (ThemeGenerator) |
| 색상 정의 충돌 | 3곳 | 0 (Colors 싱글톤) |
| 폰트 정의 중복 | 3곳 | 0 (Typography 토큰) |
| 위젯 평균 라인 수 | 1,800+ | < 500 |

### 9.3 정성적 지표

- [ ] 새로운 거래소 추가 시 1일 이내 완료 가능
- [ ] 새로운 전략 추가 시 독립적 개발 가능
- [ ] UI 변경 시 비즈니스 로직 영향 없음
- [ ] 단위 테스트 독립 실행 가능
- [ ] **새 UI 컴포넌트 추가 시 디자인 토큰만 참조**
- [ ] **테마 변경 시 tokens.py 수정만으로 전체 적용**

---

## 10. 참고 자료

### 10.1 관련 문서
- `README.md`: 프로젝트 개요
- `STRATEGY_GUIDE.md`: 전략 가이드
- `CHANGELOG.md`: 변경 이력

### 10.2 기술 참조
- Clean Architecture (Robert C. Martin)
- Strangler Fig Pattern (Martin Fowler)
- PyQt5 Best Practices
- **Design Tokens (Nathan Curtis)**
- **Atomic Design (Brad Frost)**

---

## 11. 부록

### A. 파일별 분리 매핑

<details>
<summary>클릭하여 전체 매핑 보기</summary>

| 현재 파일 | 목표 파일 | 상태 |
|-----------|-----------|------|
| `core/strategy_core.py` | `trading/core/signals.py`, `trading/core/indicators.py` | 예정 |
| `core/unified_bot.py` | `trading/bot/*.py` | 예정 |
| `core/optimizer.py` | `trading/backtest/optimizer.py` | 부분완료 |
| `GUI/optimization_widget.py` | `ui/widgets/optimization.py` | 부분완료 |
| `GUI/trading_dashboard.py` | `ui/widgets/dashboard.py` | 예정 |
| `GUI/backtest_widget.py` | `ui/widgets/backtest.py` | 부분완료 |
| `GUI/constants.py` | `config/constants.py` | 예정 |
| `exchanges/*_exchange.py` | `exchanges/adapters/*.py` | 예정 |

</details>

### B. import 경로 변경 가이드

<details>
<summary>클릭하여 변경 가이드 보기</summary>

```python
# 지표 함수
# Before
from core.strategy_core import calculate_rsi, calculate_atr
# After
from trading.core.indicators import calculate_rsi, calculate_atr

# 백테스트
# Before
from core.unified_backtest import UnifiedBacktest
# After
from trading.backtest.engine import BacktestEngine

# 최적화
# Before
from core.optimizer import OptimizerEngine
# After
from trading.backtest.optimizer import OptimizerEngine

# 거래소
# Before
from exchanges.bybit_exchange import BybitExchange
# After
from exchanges.adapters.bybit import BybitExchange

# 상수
# Before
from GUI.constants import TF_MAPPING
# After
from config.constants import TF_MAPPING
```

</details>

---

---

## 12. Phase 0 빠른 시작 가이드

### 즉시 실행 가능한 작업

```bash
# 1. 디자인 시스템 디렉토리 생성
mkdir -p ui/design_system

# 2. 기본 파일 생성
touch ui/design_system/__init__.py
touch ui/design_system/tokens.py
touch ui/design_system/theme.py
```

### tokens.py 핵심 코드

```python
# ui/design_system/tokens.py
from dataclasses import dataclass

@dataclass(frozen=True)
class ColorTokens:
    bg_base: str = "#0d1117"
    bg_surface: str = "#161b22"
    bg_elevated: str = "#21262d"
    accent_primary: str = "#00d4aa"
    text_primary: str = "#f0f6fc"
    success: str = "#3fb950"
    danger: str = "#f85149"

Colors = ColorTokens()
```

### 적용 테스트

```python
# 테스트 코드
from ui.design_system.tokens import Colors
print(f"Primary Accent: {Colors.accent_primary}")
# 출력: Primary Accent: #00d4aa
```

---

**문서 작성**: AI Assistant  
**버전**: 2.0 (UI First 개편안)  
**검토자**: -  
**승인자**: -  
**최종 수정**: 2026-01-13
