# 📊 최적화 전략 가이드 (MACD + W/M 패턴)

## 🎯 전략 구조 요약

```
┌─────────────────────────────────────────────────────────────┐
│                    AlphaX7Core 전략                          │
├─────────────────────────────────────────────────────────────┤
│  1. MACD 히스토그램 → H/L 포인트 추출                         │
│  2. W/M 패턴 탐지 (L-H-L = W패턴, H-L-H = M패턴)              │
│  3. MTF 필터 (상위 타임프레임 추세 확인)                       │
│  4. ATR 기반 손절/트레일링                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 📈 신호 생성 과정

### Step 1: MACD 계산
```python
# 파라미터
macd_fast = 6      # EMA 빠른선 (기본 12 → 6으로 민감하게)
macd_slow = 18     # EMA 느린선 (기본 26 → 18로 빠르게)
macd_signal = 7    # 시그널선 (기본 9 → 7로 민감하게)

# 계산
MACD = EMA(close, fast) - EMA(close, slow)
Signal = EMA(MACD, signal)
Histogram = MACD - Signal
```

### Step 2: H/L 포인트 추출
```
MACD Histogram > 0 구간 → 해당 구간의 최고점(H) 추출
MACD Histogram < 0 구간 → 해당 구간의 최저점(L) 추출
```

### Step 3: W/M 패턴 탐지
```
W 패턴 (Long 신호):  L1 → H → L2  (더블바텀)
M 패턴 (Short 신호): H1 → L → H2  (더블탑)

조건:
- |L2 - L1| / L1 < pattern_tolerance (기본 5%)
- 패턴 확정 후 entry_validity_hours 이내 (기본 6시간)
```

### Step 4: MTF 필터 (Multi-Timeframe)
```
filter_tf = '4h' 또는 '6h' 또는 '12h' 또는 '1d'

Long 진입: filter_tf 추세 = 'up' 일 때만
Short 진입: filter_tf 추세 = 'down' 일 때만
```

---

## 🔧 파라미터 → 결과 영향도

### 📊 MACD 파라미터

| 파라미터 | 범위 | 영향 | 결과에 미치는 효과 |
|----------|------|------|-------------------|
| `macd_fast` | 4~12 | 신호 민감도 | ↓작을수록: 신호 많음, 승률↓, 거래수↑ |
| `macd_slow` | 16~30 | 신호 안정성 | ↑클수록: 신호 적음, 승률↑, 거래수↓ |
| `macd_signal` | 5~11 | 타이밍 | ↓작을수록: 빠른 진입, 변동성↑ |

**최적 조합 (현재)**: `fast=6, slow=18, signal=7` → 민감한 설정

---

### 📊 필터/진입 파라미터

| 파라미터 | 범위 | 영향 | 결과에 미치는 효과 |
|----------|------|------|-------------------|
| `filter_tf` | 2h~1d | **매매빈도** | ↑길수록: 거래수↓↓, 승률↑ |
| `entry_validity_hours` | 6~96h | **신호 유효기간** | ↑길수록: 거래수↑, 노이즈↑ |
| `pattern_tolerance` | 3~10% | 패턴 허용 오차 | ↑클수록: 신호 많음, 정확도↓ |

**핵심**: `filter_tf`가 **매매빈도에 가장 큰 영향**

```
filter_tf = 2h  → 거래빈도 ~2.2회/일 ❌
filter_tf = 4h  → 거래빈도 ~1.7회/일 ❌
filter_tf = 6h  → 거래빈도 ~1.3회/일 ❌
filter_tf = 12h → 거래빈도 ~0.6회/일 △
filter_tf = 1d  → 거래빈도 ~0.3회/일 ✅
```

---

### 📊 리스크 관리 파라미터

| 파라미터 | 범위 | 영향 | 결과에 미치는 효과 |
|----------|------|------|-------------------|
| `atr_mult` | 1.0~3.0 | 손절폭 | ↑클수록: MDD↓, 승률↑, 수익↓ |
| `trail_start_r` | 0.5~1.5 | 트레일링 시작 | ↓작을수록: 빠른 익절, 수익↓ |
| `trail_dist_r` | 0.02~0.15 | 트레일링 거리 | ↑클수록: 수익↑, 변동성↑ |
| `leverage` | 1~10 | 레버리지 | ↑클수록: 수익↑, MDD↑↑ |

**MDD 공식**: `실제MDD = 기본MDD × leverage`

---

## 📋 프리셋 비교

### MACD 프리셋 (`bybit_btcusdt_1h_macd.json`)
```json
{
  "결과": {
    "승률": 83.75%,     // ✅ 목표 80% 달성
    "MDD": 10.86%,      // ✅ 목표 20% 이하
    "거래수": 2,216회,  // ⚠️ 약 1.0회/일
    "등급": "A"
  },
  "파라미터": {
    "atr_mult": 1.5,
    "trail_start": 1.2,
    "trail_dist": 0.03
  }
}
```

### ADX-DI 프리셋 (`bybit_btcusdt_1h_adxdi.json`)
```json
{
  "결과": {
    "승률": 78.81%,     // ⚠️ 목표 80% 미달
    "MDD": 11.05%,      // ✅ 목표 20% 이하
    "거래수": 2,572회,  // ⚠️ 약 1.2회/일
    "등급": "C"
  },
  "파라미터": {
    "atr_mult": 1.5,
    "trail_start": 1.2,
    "trail_dist": 0.03
  }
}
```

### 최신 테스트 결과 (Quick 최적화)
```
🏆 S등급 (#6):
  - filter_tf: 4h
  - leverage: 1
  - atr_mult: 2.5
  - 승률: 82.5% ✅
  - MDD: 6.7% ✅
  - 거래빈도: 1.73회/일 ❌ (목표: 0.5회/일)
```

---

## 🎯 목표 달성을 위한 권장 설정

### 현재 목표
| 지표 | 목표 | 현재 최고 |
|------|------|----------|
| 승률 | ≥ 80% | 82.5% ✅ |
| MDD | ≤ 20% | 6.7% ✅ |
| 매매빈도 | ≤ 0.5회/일 | 1.28회/일 ❌ |

### 권장 파라미터 변경

```python
# 현재 (문제)
'filter_tf': ['4h', '6h']
'entry_validity_hours': [6.0, 12.0]

# 권장 (해결책)
'filter_tf': ['12h', '1d']              # 대폭 상향
'entry_validity_hours': [48.0, 72.0]    # 2~3일로 늘림
```

### 예상 효과
```
매매빈도: 1.7회/일 → 0.3~0.5회/일 ✅
승률: 유지 또는 소폭 상승
MDD: 유지
```

---

## 📊 파라미터 영향도 매트릭스

```
              │ 승률  │ MDD   │ 수익률 │ 거래수 │
──────────────┼───────┼───────┼────────┼────────┤
macd_fast ↓   │  ↓↓   │  ↑    │   ↑    │  ↑↑   │
macd_slow ↑   │  ↑    │  ↓    │   ↓    │  ↓↓   │
filter_tf ↑   │  ↑↑   │  ↓    │   ↓    │  ↓↓↓  │ ★ 핵심
atr_mult ↑    │  ↑↑   │  ↓↓   │   ↓    │  ↓    │ ★ 핵심
leverage ↑    │  -    │  ↑↑↑  │   ↑↑   │  -    │
trail_start ↓ │  ↑    │  ↓    │   ↓↓   │  -    │
──────────────┴───────┴───────┴────────┴────────┘

★ = 결과에 가장 큰 영향
```

---

## 🔑 핵심 요약

1. **승률 올리기**: `atr_mult` ↑, `filter_tf` ↑
2. **MDD 줄이기**: `atr_mult` ↑, `leverage` ↓
3. **매매빈도 줄이기**: `filter_tf` ↑↑ (12h, 1d), `entry_validity_hours` ↑
4. **수익 올리기**: `leverage` ↑ (단, MDD 증가 감수)

**현재 가장 시급한 조치**: `filter_tf`를 `12h` 또는 `1d`로 상향하여 매매빈도 0.5회/일 달성

---

*작성일: 2026-01-14*
*버전: 1.0*
