# TwinStar Quantum 프로젝트 아키텍처 문서

**버전:** v1.0.2  
**작성일:** 2025-12-19  
**상태:** Production Ready

---

## 📋 목차

1. [프로젝트 구조](#1-프로젝트-구조)
2. [시스템 아키텍처 다이어그램](#2-시스템-아키텍처-다이어그램)
3. [Core 모듈](#3-core-모듈)
4. [GUI 모듈](#4-gui-모듈)
5. [Exchange 모듈](#5-exchange-모듈)
6. [Strategy 모듈](#6-strategy-모듈)
7. [Utility 모듈](#7-utility-모듈)
8. [위젯 레이아웃](#8-위젯-레이아웃)
9. [데이터 흐름](#9-데이터-흐름)
10. [빌드 구성](#10-빌드-구성)

---

## 1. 프로젝트 구조

```
c:\매매전략\
│
├── 📁 core/                    # 핵심 비즈니스 로직
│   ├── __init__.py
│   ├── license_guard.py        # 라이센스 보안 (563줄)
│   ├── optimizer.py            # 파라미터 최적화
│   ├── strategy_core.py        # 전략 엔진 (AlphaX7)
│   └── unified_bot.py          # 통합 봇 (127,517B)
│
├── 📁 GUI/                     # 그래픽 인터페이스
│   ├── staru_main.py           # 메인 윈도우 (30KB)
│   ├── trading_dashboard.py    # 매매 대시보드
│   ├── backtest_widget.py      # 백테스트 (41KB)
│   ├── optimization_widget.py  # 최적화 (33KB)
│   ├── settings_widget.py      # 설정 (33KB)
│   ├── history_widget.py       # 거래 내역 (39KB)
│   ├── pc_license_dialog.py    # 라이센스 다이얼로그
│   ├── help_popup.py           # 도움말 팝업
│   ├── tier_popup.py           # 등급 정보
│   └── ... (50+ 파일)
│
├── 📁 exchanges/               # 거래소 어댑터
│   ├── base_exchange.py        # 추상 기본 클래스
│   ├── bybit_exchange.py       # Bybit (19KB)
│   ├── binance_exchange.py     # Binance
│   ├── okx_exchange.py         # OKX
│   ├── bitget_exchange.py      # Bitget
│   ├── bingx_exchange.py       # BingX
│   ├── upbit_exchange.py       # 업비트
│   ├── bithumb_exchange.py     # 빗썸
│   ├── lighter_exchange.py     # Lighter (DEX)
│   └── ccxt_exchange.py        # CCXT 래퍼
│
├── 📁 strategies/              # 전략 모듈
│   ├── wm_pattern_strategy.py  # W/M 패턴 전략
│   ├── parameter_optimizer.py  # 파라미터 최적화
│   └── strategy_loader.py      # 전략 로더
│
├── 📁 locales/                 # 다국어 지원
│   ├── ko.json                 # 한국어
│   ├── en.json                 # 영어
│   └── lang_manager.py         # 언어 관리자
│
├── 📁 utils/                   # 유틸리티
│   ├── bot_data_utils.py       # 봇 데이터 처리
│   ├── data_utils.py           # 데이터 유틸
│   └── preset_manager.py       # 프리셋 관리
│
├── 📁 config/presets/          # 프리셋 설정
├── 📁 data/cache/              # 캐시 데이터
├── 📁 logs/                    # 로그 파일
│
├── paths.py                    # 경로 관리
├── license_manager.py          # 라이센스 관리
├── telegram_notifier.py        # 텔레그램 알림
├── staru_clean.spec            # PyInstaller 설정
└── requirements.txt            # 의존성
```

---

## 2. 시스템 아키텍처 다이어그램

### 2.1 전체 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                         TwinStar Quantum                              │
│                         (staru_main.py)                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │
│  │ Trading │ │Backtest │ │ Optim.  │ │ History │ │Settings │        │
│  │Dashboard│ │ Widget  │ │ Widget  │ │ Widget  │ │ Widget  │        │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘        │
│       │           │           │           │           │              │
├───────┴───────────┴───────────┴───────────┴───────────┴──────────────┤
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                         Core Layer                              │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │   UnifiedBot    │   AlphaX7Core   │   LicenseGuard            │  │
│  │   (unified_bot) │   (strategy)    │   (license_guard)         │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                      Exchange Layer                             │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │ Bybit │ Binance │ OKX │ Bitget │ BingX │ Upbit │ Bithumb │ DEX │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 라이센스 흐름

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   사용자     │────▶│ PCLicense   │────▶│ LicenseGuard│
│   로그인     │     │   Dialog    │     │   login()   │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                                │
                    ┌───────────────────────────┘
                    ▼
            ┌─────────────┐     ┌─────────────┐
            │   서버 API   │◀───│  get_token  │
            │ (PHP 백엔드) │     │  get_params │
            └─────────────┘     └─────────────┘
                    │
                    ▼
            ┌─────────────┐     ┌─────────────┐
            │ UnifiedBot  │◀───│ can_trade() │
            │execute_entry│     │  등급 체크  │
            └─────────────┘     └─────────────┘
```

### 2.3 매매 실행 흐름

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ WebSocket│───▶│  Candle  │───▶│ Strategy │───▶│  Signal  │
│  데이터  │    │Aggregator│    │  Core    │    │ 감지     │
└──────────┘    └──────────┘    └──────────┘    └────┬─────┘
                                                      │
    ┌─────────────────────────────────────────────────┘
    ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ License  │───▶│  Entry   │───▶│ Exchange │───▶│  주문    │
│  Check   │    │ Execute  │    │  API     │    │  체결    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

---

## 3. Core 모듈

### 3.1 unified_bot.py (127KB)

**역할:** 통합 트레이딩 봇 엔진

| 클래스/함수 | 라인 | 기능 |
|-------------|------|------|
| `UnifiedBot` | - | 메인 봇 클래스 |
| `__init__` | ~350 | 초기화, license_guard 연동 |
| `_can_trade` | ~1635 | 라이센스 기반 매매 가능 여부 |
| `execute_entry` | ~2080 | 진입 주문 실행 |
| `execute_exit` | ~2200 | 청산 주문 실행 |
| `detect_signal` | ~1260 | 시그널 감지 |
| `run` | ~2300 | 메인 루프 |

### 3.2 strategy_core.py (39KB)

**역할:** Alpha-X7 전략 엔진

| 클래스/함수 | 기능 |
|-------------|------|
| `AlphaX7Core` | 전략 코어 클래스 |
| `_extract_all_signals` | W/M 패턴 감지 |
| `_detect_pattern` | 패턴 검증 |
| `manage_position_realtime` | 실시간 포지션 관리 |

### 3.3 license_guard.py (18KB)

**역할:** 라이센스 보안 및 서버 통신

| 메서드 | 기능 |
|--------|------|
| `check_server_status()` | 서버 상태 확인 |
| `login(email)` | 이메일 로그인 |
| `get_token()` | JWT 토큰 발급 |
| `get_encrypted_params()` | 암호화된 파라미터 |
| `can_trade()` | 매매 가능 여부 |
| `create_upgrade_session()` | 업그레이드 세션 |

### 3.4 optimizer.py (22KB)

**역할:** 파라미터 최적화

| 클래스/함수 | 기능 |
|-------------|------|
| `GridOptimizer` | 그리드 서치 |
| `RandomOptimizer` | 랜덤 서치 |
| `optimize()` | 최적화 실행 |

---

## 4. GUI 모듈

### 4.1 메인 윈도우 - staru_main.py (30KB)

**클래스:** `StarUWindow` / `MainWindow`

| 구성요소 | 크기 | 기능 |
|----------|------|------|
| 메인 윈도우 | 1400 x 900 | 전체 프레임 |
| 헤더 바 | 높이 40px | 로고, 언어, 등급, 업그레이드 |
| 탭 위젯 | 가변 | 기능별 탭 |

**탭 구성:**

```
┌───────┬───────┬───────┬───────┬───────┬───────┐
│매매📈 │백테스트│최적화 │히스토리│데이터 │설정⚙️│
└───────┴───────┴───────┴───────┴───────┴───────┘
```

### 4.2 TradingDashboard (30KB)

**크기:** 전체 탭 영역

| 영역 | 구성 |
|------|------|
| 좌측 패널 | 거래소/심볼 선택, 봇 상태 |
| 중앙 패널 | 차트, 포지션 |
| 우측 패널 | 로그, 통계 |

### 4.3 BacktestWidget (41KB)

**크기:** 전체 탭 영역

| 영역 | 구성 |
|------|------|
| 상단 | 프리셋 선택 (🔄💾🗑 버튼) |
| 중앙 | 파라미터 입력 폼 |
| 하단 | 결과 차트/테이블 |

### 4.4 OptimizationWidget (33KB)

**크기:** 전체 탭 영역

| 영역 | 구성 |
|------|------|
| 파라미터 범위 | Min/Max/Step 입력 |
| 옵션 | Fast/Full 선택 |
| 결과 | 최적 파라미터 표시 |

### 4.5 SettingsWidget (33KB)

**크기:** 전체 탭 영역 (스크롤)

| 섹션 | 높이 | 기능 |
|------|------|------|
| 거래소 API | 280px | API 키 입력 |
| 텔레그램 | 180px | 알림 설정 |
| 라이센스 | 가변 | 등급 정보, 업그레이드 |

### 4.6 팝업 다이얼로그

| 파일 | 크기 | 기능 |
|------|------|------|
| `pc_license_dialog.py` | 540 x 660 | 로그인/등록 |
| `help_popup.py` | 500 x 400 | 도움말 (5탭) |
| `tier_popup.py` | 500 x 600 | 등급 비교표 |
| `payment_dialog.py` | 450 x 480 | 결제 안내 |

---

## 5. Exchange 모듈

### 5.1 base_exchange.py (추상 클래스)

```python
class BaseExchange(ABC):
    @abstractmethod
    def connect(self) -> bool
    
    @abstractmethod
    def get_klines(self, symbol, timeframe, limit) -> pd.DataFrame
    
    @abstractmethod
    def place_market_order(self, symbol, side, amount) -> dict
    
    @abstractmethod
    def close_position(self, symbol, side, amount) -> dict
    
    @abstractmethod
    def get_balance(self) -> float
    
    @abstractmethod
    def set_leverage(self, symbol, leverage) -> bool
```

### 5.2 거래소별 구현

| 파일 | 크기 | 거래소 | 타입 |
|------|------|--------|------|
| `bybit_exchange.py` | 19KB | Bybit | CEX 선물 |
| `binance_exchange.py` | 11KB | Binance | CEX 선물 |
| `okx_exchange.py` | 11KB | OKX | CEX 선물 |
| `bitget_exchange.py` | 10KB | Bitget | CEX 선물 |
| `bingx_exchange.py` | 10KB | BingX | CEX 선물 |
| `upbit_exchange.py` | 9KB | 업비트 | CEX 현물 |
| `bithumb_exchange.py` | 11KB | 빗썸 | CEX 현물 |
| `lighter_exchange.py` | 11KB | Lighter | DEX |
| `ccxt_exchange.py` | 20KB | CCXT | 범용 래퍼 |

---

## 6. Strategy 모듈

### 6.1 wm_pattern_strategy.py

**전략:** W/M 더블 패턴

```
W패턴 (Long):          M패턴 (Short):
    ╭╮   ╭╮              ╭───╮
   ╱  ╲ ╱  ╲            ╱     ╲
  ╱    ╳    ╲──▶ BUY   ╱       ╲
 ╱    ╱ ╲    ╲        ╱    ╳    ╲
╱    ╱   ╲    ╲      ╱    ╱ ╲    ╲
         neckline    ╰───╯   ╰───╯──▶ SELL
```

### 6.2 parameter_optimizer.py

| 파라미터 | 기본값 | 범위 |
|----------|--------|------|
| `atr_mult` | 1.5 | 1.0 ~ 3.0 |
| `rsi_period` | 14 | 7 ~ 21 |
| `trail_start_r` | 0.8 | 0.4 ~ 1.2 |
| `trail_dist_r` | 0.5 | 0.1 ~ 0.8 |
| `entry_validity_hours` | 48 | 6 ~ 72 |

---

## 7. Utility 모듈

### 7.1 paths.py

**경로 관리 클래스**

```python
class Paths:
    BASE = get_base_path()           # 프로젝트 루트
    CACHE = BASE / 'data' / 'cache'  # 캐시 데이터
    LOGS = BASE / 'logs'             # 로그 파일
    CONFIG = BASE / 'config'         # 설정 파일
    USER = BASE / 'user'             # 사용자 데이터
```

### 7.2 locales/

**다국어 지원 (ko.json, en.json)**

```python
from locales import t

label = QLabel(t('tabs.trading', 'Trading'))
# ko: "매매"
# en: "Trading"
```

### 7.3 preset_manager.py

**프리셋 관리**

| 함수 | 기능 |
|------|------|
| `load_preset(name)` | 프리셋 로드 |
| `save_preset(name, params)` | 프리셋 저장 |
| `list_presets()` | 프리셋 목록 |

---

## 8. 위젯 레이아웃

### 8.1 메인 윈도우 구조

```
┌─────────────────────────────────────────────────────────────┐
│ ⭐ TwinStar Quantum v1.0.2  │ 🌐 │ 등급 │ D-day │ 💳 업그레이드 │
├─────────────────────────────────────────────────────────────┤
│ ┌───────┬───────┬───────┬───────┬───────┬───────┐          │
│ │ 매매  │백테스트│ 최적화 │히스토리│ 데이터 │ 설정  │          │
│ └───────┴───────┴───────┴───────┴───────┴───────┘          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│                     [탭 콘텐츠 영역]                          │
│                                                              │
│                      가변 크기                               │
│                   (최소 800 x 600)                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘

크기: 1400 x 900 (최소: 1024 x 768)
```

### 8.2 TradingDashboard 레이아웃

```
┌─────────────────────────────────────────────────────────────┐
│ 거래소: [▼ Bybit  ] 심볼: [▼ BTCUSDT] 금액: [____] [Start] │
├───────────────┬─────────────────────────┬───────────────────┤
│               │                         │                   │
│   봇 상태     │        차트 영역        │    로그 패널     │
│   카드        │                         │                   │
│   (200px)     │     (가변 크기)         │    (280px)        │
│               │                         │                   │
│   • Running   │    [캔들차트]           │   [실시간 로그]   │
│   • Pending   │    [지표 표시]          │                   │
│   • Position  │                         │                   │
│               │                         │                   │
├───────────────┴─────────────────────────┴───────────────────┤
│ [포지션 정보] | [통계 패널]                                  │
└─────────────────────────────────────────────────────────────┘
```

### 8.3 BacktestWidget 레이아웃

```
┌─────────────────────────────────────────────────────────────┐
│ 프리셋: [▼ default      ] [🔄 80px] [💾 70px] [🗑 60px]     │
├───────────────────────────────┬─────────────────────────────┤
│                               │                             │
│   파라미터 입력 폼            │      결과 영역              │
│                               │                             │
│   ATR Multiple: [1.5 ▼]       │   [수익률 차트]             │
│   RSI Period:   [14  ▼]       │                             │
│   Trail Start:  [0.8 ▼]       │   [거래 히스토리]           │
│   ...                         │                             │
│                               │   승률: 65%                 │
│   [Run Backtest]              │   수익률: +45%              │
│                               │   MDD: -12%                 │
└───────────────────────────────┴─────────────────────────────┘
```

---

## 9. 데이터 흐름

### 9.1 캔들 데이터 흐름

```
[거래소 API]
     │
     ▼
[WebSocket / REST]
     │
     ▼
[CandleAggregator] ──▶ [Parquet 캐시]
     │                  (data/cache/*.parquet)
     ▼
[IndicatorGenerator]
     │
     ▼
[DataFrame with Indicators]
     │
     ├──▶ [BacktestWidget]
     │
     └──▶ [UnifiedBot.detect_signal]
```

### 9.2 설정 데이터 흐름

```
[config/presets/*.json]
         │
         ▼
   [PresetManager]
         │
         ├──▶ [BacktestWidget] ──▶ UI 표시
         │
         └──▶ [UnifiedBot] ──▶ 매매 파라미터
```

---

## 10. 빌드 구성

### 10.1 staru_clean.spec

**PyInstaller 설정**

| 항목 | 값 |
|------|-----|
| 진입점 | `GUI/staru_main.py` |
| 이름 | `TwinStar_Quantum` |
| 아이콘 | `GUI/assets/icon.ico` |
| UPX | 활성화 |
| 콘솔 | 활성화 (디버그용) |

### 10.2 hiddenimports

```python
hiddenimports = [
    # Core
    'core.license_guard',
    'core.unified_bot',
    'core.strategy_core',
    
    # GUI
    'GUI.help_popup',
    'GUI.tier_popup',
    'GUI.pc_license_dialog',
    ...
    
    # Crypto
    'Crypto.Cipher.AES',
    'Crypto.Util.Padding',
    
    # Exchanges
    'pybit', 'binance', 'ccxt',
    ...
]
```

### 10.3 datas

```python
datas = [
    ('GUI/assets', 'GUI/assets'),
    ('config/presets', 'config/presets'),
    ('locales/*.json', 'locales'),
    ('GUI/styles.qss', 'GUI'),
]
```

### 10.4 빌드 명령

```powershell
# 빌드
pyinstaller staru_clean.spec --clean

# 출력
dist/TwinStar_Quantum/TwinStar_Quantum.exe
```

---

## 📊 파일 통계

| 모듈 | 파일 수 | 총 크기 |
|------|---------|---------|
| core/ | 5 | 208KB |
| GUI/ | 58 | 650KB |
| exchanges/ | 13 | 155KB |
| strategies/ | 5 | 35KB |
| locales/ | 4 | 24KB |
| utils/ | 4 | 21KB |
| 루트 | 15+ | 100KB+ |
| **총계** | **100+** | **~1.2MB** |

---

## 🔧 의존성

### 주요 패키지

| 패키지 | 버전 | 용도 |
|--------|------|------|
| PyQt5 | 5.15+ | GUI |
| pandas | 2.0+ | 데이터 처리 |
| pybit | 5.0+ | Bybit API |
| binance | 1.0+ | Binance API |
| ccxt | 4.0+ | 범용 거래소 |
| pycryptodome | 3.19+ | 암호화 |
| pyarrow | 14.0+ | Parquet |
| ta | 0.10+ | 기술 지표 |

---

**작성:** Antigravity AI  
**마지막 업데이트:** 2025-12-19 21:41
