# 📊 매매 탭 개선 계획서

> **작성일**: 2026-01-15
> **대상**: TwinStar-Quantum GUI 매매 대시보드
> **목표**: 실시간 매매 UX/UI 개선 및 기능 확장

---

## 📋 목차

1. [현황 분석](#현황-분석)
2. [개선 목표](#개선-목표)
3. [Phase별 개선 계획](#phase별-개선-계획)
4. [구현 우선순위](#구현-우선순위)
5. [기술 스택](#기술-스택)
6. [예상 효과](#예상-효과)

---

## 🔍 현황 분석

### 현재 구조

#### 파일 구성
```
GUI/
├── trading_dashboard.py          # 레거시 v1 (deprecated)
├── trading_dashboard_v2.py       # v2 (deprecated)
├── trading_dashboard_v3.py       # ✅ 현재 사용 중 (v4.3)
├── live_trading_manager.py       # 실매매 관리자 (스텁)
└── components/
    ├── bot_control_card.py       # ✅ 봇 제어 카드 (Phase 3-7 완료)
    ├── position_table.py         # 포지션 테이블
    ├── trade_panel.py            # 트레이딩 패널
    └── status_card.py            # 상태 카드
```

#### 레이아웃 구조 (v4.3)

```
┌─────────────────────────────────────────────────────────────┐
│  💰 Wallet    │  📈 Daily PnL  │  🤖 Active   │  🛡️ Risk    │  ← HUD (높이 60~100px)
│  $0.00        │  $0.00         │  0 Moving    │  Safe       │
└─────────────────────────────────────────────────────────────┘
┌──────────────────────────┬──────────────────────────────────┐
│  Control Center (60%)    │  Monitor & Logs (40%)            │
│  ┌────────────────────┐  │  ┌──────────────────────────┐    │
│  │ 🎯 Single Trade    │  │  │ Market Monitor           │    │
│  │ 🔍 Multi Scan      │  │  │ No Active Positions      │    │
│  │ [Tab 선택]         │  │  │                          │    │
│  │                    │  │  └──────────────────────────┘    │
│  │ - 거래소 선택      │  │  ┌──────────────────────────┐    │
│  │ - 심볼 선택        │  │  │ System Logs              │    │
│  │ - 레버리지/시드    │  │  │ [실시간 로그]            │    │
│  │ - 자본모드         │  │  │                          │    │
│  │ - 프리셋           │  │  └──────────────────────────┘    │
│  │ [▶ Start/■ Stop]   │  │                                  │
│  └────────────────────┘  │                                  │
└──────────────────────────┴──────────────────────────────────┘
```

### 장점

1. ✅ **깔끔한 레이아웃**: 2열 스플리터 구조로 공간 효율적
2. ✅ **토큰 기반 디자인**: `BotControlCard`는 Phase 3-7에서 디자인 시스템 통합 완료
3. ✅ **탭 기반 제어**: Single/Multi 모드 분리
4. ✅ **HUD 카드**: 핵심 지표 한눈에 표시

### 문제점

#### 1. 기능 부족
- ❌ **포지션 모니터링 없음**: "No Active Positions" 플레이스홀더만 존재
- ❌ **실시간 가격 없음**: 현재 가격/PnL 업데이트 없음
- ❌ **차트 없음**: 시각적 시장 분석 불가
- ❌ **거래 내역 없음**: 과거 거래 기록 확인 불가

#### 2. UX 문제
- ❌ **단일 봇만 제어 가능**: 여러 코인 동시 운영 불가
- ❌ **상태 피드백 부족**: 봇 실행 중 상태 표시 미흡
- ❌ **알림 없음**: 중요 이벤트(진입/익절/손절) 알림 없음

#### 3. 디자인 일관성
- ⚠️ **일부 컴포넌트 레거시**: `position_table.py`, `trade_panel.py`는 토큰 기반이지만 최적화 필요
- ⚠️ **v1/v2 중복**: 레거시 파일 정리 필요

---

## 🎯 개선 목표

### 핵심 목표

1. **실시간 모니터링 강화**
   - 포지션 현황 실시간 업데이트
   - 가격 변동 추적
   - PnL 실시간 계산

2. **멀티 봇 지원**
   - 여러 코인 동시 운영
   - 봇별 독립적 제어
   - 통합 대시보드

3. **시각화 개선**
   - 실시간 차트 (가격/지표)
   - 진입/익절 포인트 표시
   - 통계 그래프

4. **UX/UI 현대화**
   - 반응형 레이아웃
   - 다크 테마 최적화
   - 애니메이션 효과

---

## 📅 Phase별 개선 계획

### Phase 4-1: 레거시 정리 및 기반 작업 (1일)

**목표**: 불필요한 코드 제거 및 디자인 시스템 통합

#### 작업 내역

1. **레거시 파일 제거**
   ```
   GUI/trading_dashboard.py       → 삭제
   GUI/trading_dashboard_v2.py    → 삭제
   GUI/trading_dashboard_v3.py    → 유지 (메인)
   ```

2. **컴포넌트 디자인 시스템 통합**
   - `position_table.py`: 토큰 기반 스타일 최적화
   - `trade_panel.py`: 레이아웃 개선
   - `status_card.py`: 애니메이션 추가

3. **메인 대시보드 리팩토링**
   - `TradingDashboardV3` → `TradingDashboard` (v5.0)
   - 코드 정리 및 주석 추가
   - 타입 힌트 완성

#### 예상 결과
- 코드 줄 수: -500줄 (레거시 제거)
- Pyright 에러: 0개 유지
- 파일 수: 3개 감소

---

### Phase 4-2: 멀티 봇 제어 시스템 (2일)

**목표**: 여러 코인을 동시에 운영할 수 있는 시스템 구축

#### 기능 설계

##### 1. 봇 목록 관리

```python
# 새로운 컴포넌트: GUI/components/bot_list_widget.py
class BotListWidget(QWidget):
    """멀티 봇 제어 위젯"""

    def __init__(self):
        self.bot_cards: List[BotControlCard] = []
        self.max_bots = 10

    def add_bot(self, exchange: str, symbol: str):
        """새 봇 추가"""
        ...

    def remove_bot(self, bot_id: str):
        """봇 제거"""
        ...

    def get_active_bots(self) -> List[BotControlCard]:
        """실행 중인 봇 목록"""
        ...
```

##### 2. 레이아웃 변경

```
┌─────────────────────────────────────────────────────────────┐
│  💰 Total      │  📈 Daily PnL  │  🤖 Active   │  🛡️ Risk    │
│  $1,234.56     │  +$123.45      │  3/10 Running│  Moderate   │
└─────────────────────────────────────────────────────────────┘
┌──────────────────────────┬──────────────────────────────────┐
│  🤖 Bot Control (60%)    │  📊 Positions & Logs (40%)       │
│  ┌────────────────────┐  │  ┌──────────────────────────┐    │
│  │ [+ Add Bot]        │  │  │ Symbol │ Mode │ PnL      │    │
│  │                    │  │  ├────────┼──────┼──────────┤    │
│  │ #1 Bybit BTCUSDT   │  │  │ BTC   │ Long │ +2.34%   │    │
│  │    ▶ Running       │  │  │ ETH   │ Short│ -0.56%   │    │
│  │    $100 → $102.34  │  │  │ SOL   │ Long │ +1.23%   │    │
│  │    [■ Stop] [⚙️]    │  │  └──────────────────────────┘    │
│  │                    │  │  ┌──────────────────────────┐    │
│  │ #2 Binance ETHUSDT │  │  │ System Logs              │    │
│  │    ▶ Running       │  │  │ [실시간 로그]            │    │
│  │    $200 → $198.88  │  │  └──────────────────────────┘    │
│  │    [■ Stop] [⚙️]    │  │                                  │
│  │                    │  │                                  │
│  │ #3 OKX SOLUSDT     │  │                                  │
│  │    ⏸️ Paused        │  │                                  │
│  │    $150 → $151.85  │  │                                  │
│  │    [▶ Start] [⚙️]   │  │                                  │
│  └────────────────────┘  │                                  │
└──────────────────────────┴──────────────────────────────────┘
```

##### 3. 봇 카드 개선

```python
# BotControlCard 확장 (기존 Phase 3-7 완료 기반)
class BotControlCard(QWidget):
    """
    개선사항:
    1. 실행 중 상태 애니메이션 (깜빡임)
    2. PnL 색상 변화 (실시간)
    3. 확장/축소 버튼 (상세 정보 토글)
    4. 빠른 설정 변경 (드롭다운)
    """

    # [NEW] 상태별 색상
    status_colors = {
        'running': Colors.success,
        'paused': Colors.warning,
        'stopped': Colors.text_muted,
        'error': Colors.danger
    }

    # [NEW] 애니메이션
    def _animate_status(self):
        """실행 중 상태 표시"""
        ...
```

#### 예상 결과
- 10개 코인 동시 운영 가능
- 봇별 독립적 시작/중지
- 통합 PnL 계산

---

### Phase 4-3: 실시간 포지션 모니터링 (2일)

**목표**: 현재 포지션 실시간 추적 및 시각화

#### 기능 설계

##### 1. 포지션 테이블 개선

```python
# GUI/components/position_table.py (기존 파일 확장)
class PositionTable(QTableWidget):
    """
    개선사항:
    1. 실시간 가격 업데이트 (1초 간격)
    2. PnL 색상 애니메이션
    3. 진입가 대비 % 표시
    4. 청산 가격 표시
    5. 빠른 청산 버튼
    """

    def __init__(self):
        super().__init__()
        self.columns = [
            "Symbol",      # 심볼
            "Side",        # Long/Short
            "Entry",       # 진입가
            "Current",     # 현재가
            "Size",        # 포지션 크기
            "Leverage",    # 레버리지
            "PnL",         # 손익 (%)
            "Liq.",        # 청산가
            "Action"       # 청산 버튼
        ]

    def update_price(self, symbol: str, price: float):
        """실시간 가격 업데이트"""
        row = self._find_row(symbol)
        if row >= 0:
            # 현재가 업데이트
            self.setItem(row, 3, QTableWidgetItem(f"${price:,.2f}"))

            # PnL 재계산
            entry = float(self.item(row, 2).text().replace('$', '').replace(',', ''))
            side = self.item(row, 1).text()
            pnl = self._calculate_pnl(entry, price, side)

            # PnL 아이템 색상 업데이트
            pnl_item = QTableWidgetItem(f"{pnl:+.2f}%")
            pnl_item.setForeground(QColor(Colors.success if pnl > 0 else Colors.danger))
            self.setItem(row, 6, pnl_item)
```

##### 2. 실시간 가격 피드

```python
# 새로운 모듈: core/price_feed.py
class PriceFeed(QThread):
    """실시간 가격 업데이트 스레드"""

    price_updated = pyqtSignal(str, float)  # symbol, price

    def __init__(self, symbols: List[str], exchange: str):
        super().__init__()
        self.symbols = symbols
        self.exchange = exchange
        self.running = True

    def run(self):
        """1초마다 가격 조회"""
        while self.running:
            for symbol in self.symbols:
                try:
                    price = self._fetch_price(symbol)
                    self.price_updated.emit(symbol, price)
                except Exception as e:
                    logger.error(f"Price fetch error: {e}")

            QThread.msleep(1000)  # 1초 대기
```

##### 3. 포지션 상세 정보 다이얼로그

```python
# 새로운 컴포넌트: GUI/dialogs/position_detail_dialog.py
class PositionDetailDialog(QDialog):
    """포지션 상세 정보 및 수정"""

    def __init__(self, position: Dict):
        super().__init__()
        self.position = position

        # 표시 정보:
        # - 진입 시간
        # - 진입가 / 현재가
        # - 손익 (%, $)
        # - 청산가
        # - 손절가 / 익절가 (수정 가능)
        # - 거래 내역 (추가매매)

        # 기능:
        # - 손절/익절 수정
        # - 일부 청산
        # - 전체 청산
```

#### 예상 결과
- 실시간 가격 추적 (1초 간격)
- PnL 자동 계산 및 색상 표시
- 포지션 상세 정보 확인 가능

---

### Phase 4-4: 실시간 차트 통합 (3일)

**목표**: 트레이딩뷰 스타일의 인터랙티브 차트 추가

#### 기능 설계

##### 1. 차트 위젯

```python
# 기존: GUI/components/interactive_chart.py (백테스트용)
# 새로운: GUI/components/live_trading_chart.py

class LiveTradingChart(QWidget):
    """실시간 매매용 차트"""

    def __init__(self, symbol: str, timeframe: str = "15m"):
        super().__init__()
        self.symbol = symbol
        self.timeframe = timeframe

        # PyQtGraph 기반 차트
        self.plot_widget = pg.PlotWidget()

        # 표시 요소:
        # 1. 캔들스틱 (실시간)
        # 2. 진입 포인트 (▲ Long, ▼ Short)
        # 3. 손절/익절 라인
        # 4. 지표 (RSI, MACD, 볼린저밴드)
        # 5. 거래량

    def add_position_marker(self, price: float, side: str, time: datetime):
        """진입 포인트 표시"""
        color = Colors.success if side == "Long" else Colors.danger
        marker = pg.ArrowItem(angle=90 if side == "Long" else -90)
        marker.setPos(time, price)
        marker.setPen(color)
        self.plot_widget.addItem(marker)

    def add_sl_tp_lines(self, stop_loss: float, take_profit: float):
        """손절/익절 라인 표시"""
        sl_line = pg.InfiniteLine(pos=stop_loss, angle=0, pen=Colors.danger)
        tp_line = pg.InfiniteLine(pos=take_profit, angle=0, pen=Colors.success)
        self.plot_widget.addItem(sl_line)
        self.plot_widget.addItem(tp_line)
```

##### 2. 차트 배치

**옵션 1: 우측 패널에 통합**
```
┌──────────────────────────┬──────────────────────────────────┐
│  🤖 Bot Control (50%)    │  📊 Chart & Positions (50%)      │
│                          │  ┌──────────────────────────┐    │
│  [봇 목록]               │  │  📈 BTC/USDT - 15m       │    │
│                          │  │  [실시간 차트]           │    │
│                          │  │                          │    │
│                          │  └──────────────────────────┘    │
│                          │  ┌──────────────────────────┐    │
│                          │  │ Positions                │    │
│                          │  └──────────────────────────┘    │
└──────────────────────────┴──────────────────────────────────┘
```

**옵션 2: 별도 탭 추가**
```
[🤖 Control] [📊 Chart] [📜 History] [⚙️ Settings]
```

##### 3. 멀티 차트 지원

```python
# 새로운 컴포넌트: GUI/components/multi_chart_widget.py
class MultiChartWidget(QWidget):
    """여러 차트 동시 표시"""

    def __init__(self, max_charts: int = 4):
        super().__init__()
        self.max_charts = max_charts
        self.charts: List[LiveTradingChart] = []

        # 2x2 그리드 레이아웃
        self.layout = QGridLayout()

    def add_chart(self, symbol: str):
        """차트 추가 (최대 4개)"""
        if len(self.charts) < self.max_charts:
            chart = LiveTradingChart(symbol)
            self.charts.append(chart)
            self._update_layout()
```

#### 예상 결과
- 실시간 캔들스틱 차트
- 진입/익절 시각적 표시
- 최대 4개 차트 동시 모니터링

---

### Phase 4-5: 알림 및 이벤트 시스템 (1일)

**목표**: 중요 이벤트 실시간 알림

#### 기능 설계

##### 1. 알림 매니저

```python
# 새로운 모듈: utils/notification_manager.py
class NotificationManager:
    """알림 관리자"""

    def __init__(self):
        self.enabled = True
        self.sound_enabled = True

    def notify(self, title: str, message: str, level: str = "info"):
        """알림 표시"""
        # 1. OS 네이티브 알림 (plyer)
        # 2. GUI 내부 토스트 메시지
        # 3. 사운드 재생 (선택)
        ...

    def notify_position_opened(self, symbol: str, side: str, price: float):
        """진입 알림"""
        self.notify(
            "🎯 Position Opened",
            f"{symbol} {side} @ ${price:,.2f}",
            level="success"
        )

    def notify_position_closed(self, symbol: str, pnl: float):
        """청산 알림"""
        level = "success" if pnl > 0 else "danger"
        self.notify(
            "✅ Position Closed" if pnl > 0 else "⚠️ Position Closed",
            f"{symbol} PnL: {pnl:+.2f}%",
            level=level
        )
```

##### 2. 토스트 메시지 위젯

```python
# 새로운 컴포넌트: GUI/components/toast_message.py
class ToastMessage(QWidget):
    """화면 우측 하단에 표시되는 알림"""

    def __init__(self, message: str, level: str = "info", duration: int = 3000):
        super().__init__()
        self.setWindowFlags(Qt.WindowType.FramelessWindowHint | Qt.WindowType.WindowStaysOnTopHint)
        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground)

        # 배경색 (레벨별)
        bg_colors = {
            "info": Colors.info_bg,
            "success": Colors.success_bg,
            "warning": Colors.warning_bg,
            "danger": Colors.danger_bg
        }

        # 애니메이션 (슬라이드 인/아웃)
        self.show_animation = QPropertyAnimation(self, b"pos")
        self.hide_animation = QPropertyAnimation(self, b"opacity")
```

##### 3. 이벤트 로그

```python
# 새로운 컴포넌트: GUI/components/event_log_widget.py
class EventLogWidget(QTableWidget):
    """이벤트 기록 테이블"""

    columns = [
        "Time",        # 시간
        "Type",        # 이벤트 타입 (진입/청산/에러)
        "Symbol",      # 심볼
        "Details",     # 상세 정보
        "Result"       # 결과 (PnL)
    ]

    def add_event(self, event_type: str, symbol: str, details: str, result: str = ""):
        """이벤트 추가"""
        row = self.rowCount()
        self.insertRow(row)

        time_str = datetime.now().strftime("%H:%M:%S")
        self.setItem(row, 0, QTableWidgetItem(time_str))

        # 타입별 아이콘
        icons = {
            "entry": "🎯",
            "exit": "✅",
            "stop_loss": "🛑",
            "error": "⚠️"
        }
        type_text = f"{icons.get(event_type, '')} {event_type}"
        self.setItem(row, 1, QTableWidgetItem(type_text))

        self.setItem(row, 2, QTableWidgetItem(symbol))
        self.setItem(row, 3, QTableWidgetItem(details))
        self.setItem(row, 4, QTableWidgetItem(result))
```

#### 예상 결과
- OS 네이티브 알림 지원
- GUI 토스트 메시지
- 이벤트 로그 기록

---

### Phase 4-6: 거래 내역 및 통계 (2일)

**목표**: 과거 거래 분석 및 통계 제공

#### 기능 설계

##### 1. 거래 내역 탭

```python
# 새로운 컴포넌트: GUI/trading/trade_history_widget.py
class TradeHistoryWidget(QWidget):
    """거래 내역 위젯"""

    def __init__(self):
        super().__init__()

        # 상단: 필터
        self.filter_bar = TradeFilterBar()

        # 중단: 테이블
        self.trade_table = TradeHistoryTable()

        # 하단: 통계 요약
        self.stats_panel = TradeStatsPanel()

    class TradeHistoryTable(QTableWidget):
        columns = [
            "ID",          # 거래 번호
            "Time",        # 시간
            "Symbol",      # 심볼
            "Side",        # Long/Short
            "Entry",       # 진입가
            "Exit",        # 청산가
            "Size",        # 크기
            "PnL",         # 손익
            "Reason"       # 청산 이유 (TP/SL/Manual)
        ]
```

##### 2. 통계 대시보드

```python
class TradeStatsPanel(QWidget):
    """거래 통계 패널"""

    def __init__(self):
        super().__init__()

        # 표시 지표:
        # - 총 거래 수
        # - 승률 (%)
        # - 평균 수익률
        # - 최대 수익 / 최대 손실
        # - Profit Factor
        # - Sharpe Ratio
        # - MDD (Maximum Drawdown)

        # 시각화:
        # - PnL 누적 그래프
        # - 승/패 분포 차트
        # - 시간대별 수익률
```

##### 3. 성과 리포트 생성

```python
# 새로운 모듈: utils/report_generator.py
class ReportGenerator:
    """PDF/HTML 리포트 생성"""

    def generate_daily_report(self, date: datetime) -> Path:
        """일일 리포트 생성"""
        # 포함 내용:
        # 1. 거래 요약
        # 2. 손익 차트
        # 3. 주요 거래 (TOP 5)
        # 4. 권장사항

        return Path("reports/daily_20260115.pdf")

    def generate_monthly_report(self, year: int, month: int) -> Path:
        """월간 리포트"""
        ...
```

#### 예상 결과
- 과거 거래 조회 및 필터링
- 상세 통계 제공
- PDF/HTML 리포트 생성

---

### Phase 4-7: 최적화 및 안정성 (1일)

**목표**: 성능 최적화 및 버그 수정

#### 작업 내역

1. **메모리 최적화**
   - 캔들 데이터 캐싱
   - 위젯 재사용
   - 타이머 정리

2. **스레드 안전성**
   - QMutex 적용
   - 신호/슬롯 검증
   - 데드락 방지

3. **에러 핸들링**
   - 거래소 API 에러 처리
   - 네트워크 재연결
   - 데이터 무결성 검증

4. **단위 테스트**
   ```python
   # tests/unit/test_trading_dashboard.py
   def test_bot_control_card():
       """BotControlCard 테스트"""
       ...

   def test_position_table_update():
       """포지션 테이블 업데이트 테스트"""
       ...
   ```

#### 예상 결과
- 메모리 사용량 -30%
- 응답 시간 < 100ms
- 안정성 99.9%

---

## 📊 구현 우선순위

### 우선순위 High (필수)

1. **Phase 4-1**: 레거시 정리 (1일)
   - 즉시 시작 가능
   - 기술 부채 해소

2. **Phase 4-2**: 멀티 봇 제어 (2일)
   - 핵심 기능
   - 사용성 대폭 향상

3. **Phase 4-3**: 실시간 포지션 모니터링 (2일)
   - 필수 기능
   - 리스크 관리 핵심

### 우선순위 Medium (권장)

4. **Phase 4-5**: 알림 시스템 (1일)
   - UX 개선
   - 사용자 편의성

5. **Phase 4-6**: 거래 내역 (2일)
   - 분석 기능
   - 장기 운영 필요

### 우선순위 Low (선택)

6. **Phase 4-4**: 실시간 차트 (3일)
   - 시각화 강화
   - 개발 시간 소요 큼

7. **Phase 4-7**: 최적화 (1일)
   - 모든 Phase 완료 후

---

## 🛠 기술 스택

### 기존 사용 중

- **GUI**: PyQt6 6.6.0+
- **차트**: PyQtGraph 0.13.3+
- **디자인**: `ui.design_system.tokens` (Phase 3 완료)
- **데이터**: Pandas, NumPy
- **거래소**: CCXT

### 신규 도입 검토

| 라이브러리 | 용도 | 필수 여부 |
|-----------|------|-----------|
| `plyer` | OS 네이티브 알림 | 선택 |
| `pyqtgraph` | 실시간 차트 (이미 사용 중) | 필수 |
| `reportlab` | PDF 리포트 생성 | 선택 |
| `websocket-client` | 실시간 가격 피드 | 권장 |

---

## 📈 예상 효과

### 정량적 효과

| 지표 | 현재 | 목표 | 개선율 |
|------|------|------|--------|
| 동시 운영 코인 | 1개 | 10개 | +900% |
| 가격 업데이트 주기 | 없음 | 1초 | ∞ |
| PnL 계산 정확도 | 수동 | 자동 | +100% |
| 사용자 클릭 수 (거래 시작) | 10회 | 3회 | -70% |

### 정성적 효과

1. **생산성 향상**
   - 여러 코인 동시 모니터링
   - 빠른 의사결정

2. **리스크 관리**
   - 실시간 손익 추적
   - 즉각적인 포지션 조정

3. **사용자 경험**
   - 직관적인 UI
   - 실시간 피드백

4. **전문성**
   - 프로 트레이더 수준의 도구
   - 경쟁력 확보

---

## 🎯 권장 실행 계획

### 빠른 개선 (1주일)

```
Day 1: Phase 4-1 (레거시 정리)
Day 2-3: Phase 4-2 (멀티 봇)
Day 4-5: Phase 4-3 (포지션 모니터링)
Day 6: Phase 4-5 (알림)
Day 7: Phase 4-7 (최적화)
```

**효과**: 핵심 기능 완성, 실전 사용 가능

### 완전한 개선 (2주일)

```
Week 1: Phase 4-1 ~ 4-3 + 4-5
Week 2: Phase 4-4 (차트) + Phase 4-6 (내역) + Phase 4-7 (최적화)
```

**효과**: 모든 기능 완성, 프로 수준 대시보드

---

## 📝 다음 단계

### 즉시 시작 가능

1. **Phase 4-1 착수**
   - `trading_dashboard.py`, `trading_dashboard_v2.py` 삭제
   - `trading_dashboard_v3.py` → `trading_dashboard.py` (v5.0) 리팩토링

2. **요구사항 확인**
   - 멀티 봇 최대 개수 (기본 10개)
   - 알림 방식 (OS 네이티브 / GUI 토스트)
   - 차트 우선순위

3. **디자인 목업**
   - 레이아웃 최종 확정
   - 사용자 피드백 수집

### 승인 대기 중

- 전체 Phase 실행 여부
- 우선순위 조정
- 일정 조율

---

## 📌 참고 문서

- [CLAUDE.md](../CLAUDE.md) - 프로젝트 개발 규칙
- [GUI_DESIGN_REFACTORING_SUMMARY.md](GUI_DESIGN_REFACTORING_SUMMARY.md) - Phase 3 GUI 개편 요약
- [GUI_EXECUTION_GUIDE.md](GUI_EXECUTION_GUIDE.md) - GUI 실행 가이드
- [WORK_LOG_20260115.txt](WORK_LOG_20260115.txt) - Session 1 & 2 작업 로그

---

**작성**: Claude Sonnet 4.5
**버전**: v1.0
**최종 수정**: 2026-01-15
